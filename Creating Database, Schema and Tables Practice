CREATE /* [OR REPLACE] */ DATABASE IF NOT EXISTS SNOWFLAKE_PRACTICE;

CREATE /* [OR REPLACE] */ SCHEMA IF NOT EXISTS SNOWFLAKE_SCHEMA;

CREATE /* [OR REPLACE] */ TABLE IF NOT EXISTS EMP(
    EMP_ID INT,
    EMP_NAME VARCHAR(30),
    EMP_ROLE VARCHAR(20)
);
